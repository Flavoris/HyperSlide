Handle lifecycle:

- On sceneWillResignActive / appWillResignActive: auto-pause and show paused overlay.
- On resume: don’t auto-unpause; require user tap.
- Persist bestScore and user settings immediately on change.
- Do not persist in-progress run (keep it simple for v1).